[project]
name = "pi-py"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pi-ai",
    "pi-agent",
]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
pi-ai = { workspace = true }
pi-agent = { workspace = true }
